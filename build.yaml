targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            eatwise_database: lib/data/database/database.dart
          schema_dir: drift_schemas/
          test_dir: test/drift/
